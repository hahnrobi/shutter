import { SelfDataLocalStorageProvider } from './self-data-local-storage-provider';

describe('SelfDataLocalStorageProvider', () => {
  it('should create an instance', () => {
    expect(new SelfDataLocalStorageProvider()).toBeTruthy();
  });
});
