import { ExistingUserDataProvider } from './existing-user-data-provider';

describe('ExistingUserDataProvider', () => {
  it('should create an instance', () => {
    expect(new ExistingUserDataProvider()).toBeTruthy();
  });
});
