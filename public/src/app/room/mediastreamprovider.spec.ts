import { Mediastreamprovider } from './mediastreamprovider';

describe('Mediastreamprovider', () => {
  it('should create an instance', () => {
    expect(new Mediastreamprovider()).toBeTruthy();
  });
});
